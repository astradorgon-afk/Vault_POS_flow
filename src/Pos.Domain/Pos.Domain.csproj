<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <!-- Internal pipeline members (e.g. PurchaseOrder.RecordReceipt) are
         consumed by the application layer and exercised by the domain tests. -->
    <InternalsVisibleTo Include="Pos.Application" />
    <InternalsVisibleTo Include="Pos.Domain.Tests" />
  </ItemGroup>

</Project>
